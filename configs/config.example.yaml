schema: instruction
paths:
  train_path: data/samples/fault_data_structured.jsonl
  val_path: null
  output_dir: outputs/flan_t5_lora
lora:
  r: 8
  alpha: 16
  dropout: 0.1
  target_modules: null
train:
  model_name: google/flan-t5-small
  max_source_length: 512
  max_target_length: 256
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  learning_rate: 0.0003
  num_train_epochs: 10
  weight_decay: 0.01
  warmup_ratio: 0.1
  logging_steps: 10
  eval_steps: 200
  save_steps: 200
  gradient_accumulation_steps: 8
  fp16: false
  bf16: false
  seed: 42
